@page
@model MyNotesModel
@{
    ViewData["Title"] = "My Notes";
}

<div class="max-w-6xl mx-auto p-4 space-y-6">

    <!-- Header & Search -->
    <div class="flex flex-col md:flex-row md:justify-between md:items-center gap-4">
        <div>
            <h2 class="text-lg font-bold flex items-center gap-2">
                My Secure Notes
            </h2>
            <p class="text-xs opacity-50">Content search is disabled for encrypted notes.</p>
        </div>

        <div class="flex flex-wrap gap-2">
            <form method="get" class="flex gap-2">
                @* Search icon *@

                <div class="join">
                    <label class="input join-item input-sm">
                        <svg class="h-[1em] opacity-50" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
                            <g
                                stroke-linejoin="round"
                                stroke-linecap="round"
                                stroke-width="2.5"
                                fill="none"
                                stroke="currentColor">
                                <circle cx="11" cy="11" r="8"></circle>
                                <path d="m21 21-4.3-4.3"></path>
                            </g>
                        </svg>
                        <input type="text" name="SearchQuery" value="@Model.SearchQuery"
                               placeholder="Search metadata or text..." class="w-full md:w-60"/>
                    </label>
                    @if (!string.IsNullOrEmpty(Model.SearchQuery))
                    {
                        <a asp-page="./MyNotes" class="btn btn-sm text-xs">Clear</a>
                    }
                    <button type="submit" class="btn btn-sm join-item btn-primary">Search</button>
                </div>
            </form>
            <a asp-page="/AddNote" class="btn btn-primary btn-sm shadow-md">+ New Note</a>
        </div>
    </div>

    @if (!Model.Notes.Any())
    {
        <div class="hero bg-base-200 rounded-box py-16">
            <div class="text-center">
                <h1 class="text-2xl font-bold opacity-30">No Notes Found</h1>
                <p class="opacity-50 text-sm mb-4">Try a different search or create a new note.</p>
                <a asp-page="/AddNote" class="btn btn-sm btn-outline">Create First Note</a>
            </div>
        </div>
    }
    else
    {
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
            @foreach (var item in Model.Notes)
            {
                <div
                    class="card bg-base-100 shadow-lg border @(item.IsExpired ? "border-error/30 opacity-80" : "border-base-300") transition-transform hover:scale-[1.02]">
                    <div class="card-body p-5">

                        <!-- Header: Code & Security Info -->
                        <div class="flex justify-between items-start mb-2">
                            <span
                                class="font-mono text-xs font-bold text-secondary bg-secondary/10 px-2 py-1 rounded">#@item.Note.Code</span>

                            <div class="flex gap-1">
                                @if (item.Note.IsEncrypted)
                                {
                                    <div class="badge badge-outline badge-xs tooltip"
                                         data-tip="Encrypted: Content safe but not searchable">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" viewBox="0 0 20 20"
                                             fill="currentColor">
                                            <path fill-rule="evenodd"
                                                  d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z"
                                                  clip-rule="evenodd"/>
                                        </svg>
                                    </div>
                                }
                                <span
                                    class="badge @(item.IsExpired ? "badge-error" : "badge-success") badge-sm text-[10px]">
                                    @(item.IsExpired ? "Expired" : "Active")
                                </span>
                            </div>
                        </div>

                        <!-- Content Preview -->
                        <div class="bg-base-200/50 rounded-lg p-3 min-h-[100px] border border-base-200">
                            <p class="text-sm line-clamp-3 italic opacity-80 break-words leading-relaxed">
                                @item.DecryptedContent
                            </p>
                        </div>

                        <!-- File Info -->
                        @if (!string.IsNullOrEmpty(item.Note.OriginalFileName))
                        {
                            <div
                                class="flex items-center gap-2 text-[11px] text-info font-medium mt-3 bg-info/5 p-2 rounded">
                                📎 <span class="truncate">@item.Note.OriginalFileName</span>
                            </div>
                        }

                        <!-- Dates -->
                        <div class="text-[10px] opacity-40 mt-4 border-t border-base-200 pt-2">
                            <span>Created: @item.Note.CreatedDateUtc.ToLocalTime().ToString("MMM dd, yyyy HH:mm")</span>
                        </div>

                        <!-- Actions -->
                        <div class="card-actions justify-end mt-4 gap-1">
                            <a asp-page="/ViewNote" asp-route-code="@item.Note.Code"
                               class="btn btn-xs btn-outline">View</a>
                            <a asp-page="/EditNote" asp-route-id="@item.Note.Id" class="btn btn-xs btn-ghost text-info">Edit</a>
                            <form method="post" asp-page-handler="Delete" asp-route-id="@item.Note.Id"
                                  asp-route-SearchQuery="@Model.SearchQuery" asp-route-P="@Model.P">
                                <button class="btn btn-xs btn-ghost text-error"
                                        onclick="return confirm('Permanently delete this note?')">
                                    Delete
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            }
        </div>

        <!-- Pagination -->
        @if (Model.TotalPages > 1)
        {
            <div class="flex justify-center mt-12">
                <div class="join shadow-md">
                    @for (int i = 1; i <= Model.TotalPages; i++)
                    {
                        <a asp-page="./MyNotes"
                           asp-route-P="@i"
                           asp-route-SearchQuery="@Model.SearchQuery"
                           class="join-item btn btn-sm @(Model.P == i ? "btn-primary" : "btn-ghost")">
                            @i
                        </a>
                    }
                </div>
            </div>
        }
    }
</div>
